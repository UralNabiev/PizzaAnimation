# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PizzaAnimation' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PizzaAnimation

  target 'PizzaAnimationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  pod 'lottie-ios', '3.5'

  target 'PizzaAnimationUITests' do
    # Pods for testing
  end

end
